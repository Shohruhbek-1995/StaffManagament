@using StaffManagament.ViewModels;

@{
    ViewBag.Title = "Staff List";
}
@*
<div class="card-deck">
    @foreach (var staff in Model.Staffs)
    {
        var photoFilePath = "~/images/" + (staff.PhotoFilePath ?? "user.png");
            <div class="card m-3" style="min-width: 18rem; max-width: 30.5%;">
                <div class="card-header">
                    <h3>@staff.FirstName @staff.LastName</h3>
                </div>
                <img class="card-img-top imageThumbnail" src="@photoFilePath" asp-append-version="true" />
                <div class="card-footer text-center">
                    <a asp-controller="home" asp-action="details" asp-route-Id="@staff.Id" class="btn btn-primary m-1">View</a>
                    <a asp-controller="home" asp-action="edit" asp-route-Id="@staff.Id" class="btn btn-primary m-1">Edit</a>
                   @* <a href="#" class="btn btn-danger m-1">Delete</a>*@ @*
                 <form asp-controller="home" asp-action="delete" asp-route-Id="@staff.Id" method="post">
                    <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this record?');">Delete</button>
                </form>
                </div>

            </div>
    }
</div>*@

<div class="card-deck" >
    @foreach (var staff in Model.Staffs)
    {
        var photoFilePath = "~/images/" + (staff.PhotoFilePath ?? "ITEAM.png");
        <div class="staff-card-container container">
          <div class="our-team m-3" style="min-width: 18rem; max-width: 30.5%;">
        <div class="picture">
               <a src="@photoFilePath" title="View Delve Profile" target="_blank" rel="noreferrer noopener" asp-append-version="true">
                   
           <div class="img-div" style="background-image: url('/_layouts/15/userphoto.aspx?size=L&username={{getUrlField (slot item  )}}');">
                    <img class="card-img-top imageThumbnail" src="@photoFilePath" asp-append-version="true" />
            </div></a>
        </div>
        <div class="team-content">
              <div class="name">@staff.FirstName @staff.LastName</div>
          @*<div class="title"> 
             Business Systems Analyst</div> *@
         @*   <span class="acronym"></span> <span class="acronym"></span>*@
          <br><br>

          <center><div class="extrainfo"> <h1>About</h1>
         <table class="dept-team" width="70%" cellspacing="3px">
           <tr>
             <td width="10%"><img src="~/images/user.png"></td>
             <td width="90%">@staff.Department</td>
           </tr>
           <tr>
             <td width="10%"><img src="https://cdn-icons-png.flaticon.com/512/1534/1534952.png"></td>
             <td width="90%">Business Systems</td>
           </tr>
           <tr>
             <td width="10%"><img src="https://cdn-icons-png.flaticon.com/512/7174/7174337.png"></td>
             <td width="90%" style="font-size: 12px;"><b>Days Off:</b> Sat, Sun</td>
           </tr>
         </table>
         </div></center>
         
                  
                   @* <a href="#" class="btn btn-danger m-1">Delete</a>*@
                
        </div>

        <div style="height: 100px;"></div>

        <ul class="social">
                    <li> <a asp-controller="home" asp-action="details" asp-route-Id="@staff.Id" class="badge bg-primary text-wrap m-1">View</a></li>
                  <li>  <a asp-controller="home" asp-action="edit" asp-route-Id="@staff.Id" class="badge btn-primary text-white  m-1">Edit</a></li>
                  <li> <form asp-controller="home" asp-action="delete" asp-route-Id="@staff.Id" method="post">
                                    <button type="submit" class="btn btn-danger text-secondary-emphasis" onclick="return confirm('Are you sure you want to delete this record?');">Delete</button>
                    </form></li>
        </ul>
      </div>
     </div>
     }
</div>